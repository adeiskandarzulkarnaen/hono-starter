import { describe, it } from 'bun:test';

describe('a RegisterUser entities', () => {
  it.todo('should throw error when payload did not contain needed property');
  it.todo('should throw error when payload did not meet data type specification');
});
